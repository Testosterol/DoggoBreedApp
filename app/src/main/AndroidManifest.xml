<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.testosterolapp.dogbreeds">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name=".core.BreedsApplication"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Dog Breeds"
        android:hardwareAccelerated="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true">
        <activity
            android:name=".main.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".breeds.BreedPicturesActivity"
            android:configChanges="screenSize|orientation|screenLayout|navigation"/>
        <activity android:name=".favorites.FavoritesActivity"
            android:configChanges="screenSize|orientation|screenLayout|navigation"/>
    </application>

</manifest>